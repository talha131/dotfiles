windows:
  - name: jump-ws-backend
    root: ~/Repos/jump-ws/neuron
    layout: main-horizontal
    panes:
      - commands:
        - nvim -S ~/.vim/sessions/jump-ws-backend
        focus: true  
      - commands:
        - make && neuron -test --logformat text
      - echo "git mu"
