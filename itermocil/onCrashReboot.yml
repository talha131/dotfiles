windows:
  - name: onCrashReboot
    root: ~/Repos/onCrashReboot
    layout: main-vertical
    panes:
      - commands:
        - echo "nvim -S ~/.vim/sessions/onCrashReboot"
        focus: true  
      - commands:
        - echo "git s"
      - commands:
        - echo "gulp"

