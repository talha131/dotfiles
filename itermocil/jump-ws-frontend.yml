windows:
  - name: jump-ws-frontend
    root: ~/Developer/jump-ws/neuron-frontend/src
    layout: main-vertical
    panes:
      - commands:
          - code ~/Developer/neuron-frontend.code-workspace
          - git s
        focus: true
      - commands:
          - yarn install && yarn start
