windows:
  - name: jump-ws-frontend
    root: ~/Repos/jump-ws/neuron-frontend/src
    layout: main-horizontal
    panes:
      - commands:
        - nvim -S ~/.local/share/nvim/sessions/jump-ws-frontend
        focus: true  
      - commands:
        - yarn install && yarn start
      - echo "git mu"

